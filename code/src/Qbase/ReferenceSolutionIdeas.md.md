# B题《多工序协同作业问题》建模思路阐述文档

> 本文档依据《2026五一杯B题建模思路.pdf》整理，目标是形成一份便于项目开发、模型说明和代码实现使用的 Markdown 版思路文档。本文档只对原建模思路进行结构化整理和表达，不改变其基本题意与建模方向。

---

## 1. 问题总体定位

B题属于典型的 **工序调度问题（Job Shop Scheduling）**。

其核心目标是在设备资源有限、工序存在先后关系、设备跨车间转运存在时间成本的条件下，为多个车间的多道工序合理分配设备，使所有任务的总完成时间最短，即最小化 **Makespan**。

从建模角度看，本题不是预测问题，也不是评价问题，而是一个以 **时间最短化** 为目标的资源约束调度优化问题。

---

## 2. 数据结构整理

### 2.1 工序流程数据

工序流程表给出了各车间的工序、设备需求、设备效率、工程量和备注信息。

| 车间 | 工序 | 设备需求与效率 | 工程量 | 备注 |
|---|---|---|---:|---|
| A | A1 缺陷填补 | 精密灌装机 200 m³/h + 自动化输送臂 250 m³/h | 300 m³ | 两设备并行 |
| A | A2 表面整平 | 高速抛光机 100 m³/h + 工业清洗机 250 m³/h | 500 m³ | 两设备并行 |
| A | A3 强度检测 | 自动传感多功能机 100 m³/h | 500 m³ | 单设备 |
| B | B1 表面清理 | 工业清洗机 100 m³/h | 120 m³ | 单设备 |
| B | B2 垫层构筑 | 精密灌装机 200 m³/h + 自动化输送臂 300 m³/h | 1500 m³ | 两设备并行 |
| B | B3 表面密封 | 精密灌装机 350 m³/h | 360 m³ | 单设备 |
| B | B4 表面整平 | 高速抛光机 120 m³/h + 自动传感多功能机 100 m³/h | 360 m³ | 两设备并行 |
| C | C1 旧涂层剥离 | 工业清洗机 250 m³/h + 自动化输送臂 250 m³/h | 720 m³ | 两设备并行 |
| C | C2 基底填充 | 精密灌装机 350 m³/h | 720 m³ | 单设备 |
| C | C3 密封覆盖 | 精密灌装机 200 m³/h + 自动化输送臂 250 m³/h | 360 m³ | 两设备并行 |
| C | C4 表面研磨 | 高速抛光机 120 m³/h + 工业清洗机 100 m³/h | 400 m³ | 两设备并行 |
| C | C5 质量检测 | 自动传感多功能机 100 m³/h | 400 m³ | 单设备 |
| D | D1 碎屑清理 | 工业清洗机 250 m³/h | 600 m³ | 单设备 |
| D | D2 基底固化 | 精密灌装机 200 m³/h + 自动化输送臂 300 m³/h | 800 m³ | 两设备并行 |
| D | D3 表面密封 | 精密灌装机 350 m³/h | 450 m³ | 单设备 |
| D | D4 表面整平 | 高速抛光机 120 m³/h + 自动传感多功能机 300 m³/h | 1500 m³ | 两设备并行 |
| D | D5 承载检测 | 自动传感多功能机 300 m³/h | 1500 m³ | 单设备 |
| D | D6 边缘修整 | 高速抛光机 100 m³/h | 700 m³ | 单设备 |
| E | E1 基础处理 | 工业清洗机 250 m³/h | 1000 m³ | 单设备 |
| E | E2 表面密封 | 精密灌装机 350 m³/h | 600 m³ | 单设备 |
| E | E3 稳定性检测 | 自动传感多功能机 300 m³/h + 工业清洗机 100 m³/h | 600 m³ | 两设备并行 |

### 2.2 C车间特殊循环说明

C车间中，C3、C4、C5 三道工序需要重复执行 3 遍。

等价处理方式为：

```text
C1 → C2 → C3_1 → C4_1 → C5_1 → C3_2 → C4_2 → C5_2 → C3_3 → C4_3 → C5_3
```

其中，C3、C4、C5 各自被拆解为 3 个独立子工序，共形成 9 道循环子工序。

该处理方式的意义是：重复执行不能简单理解为“时间乘以3”，而应作为独立工序链处理，因为每一轮都会占用设备、影响设备可用时间和后续调度。

---

## 3. 班组与设备配置

班组配置表给出了两个班组拥有的设备数量、设备移动速度和设备单价。

| 设备类型 | 班组1数量 | 班组2数量 | 移动速度 | 单价 |
|---|---:|---:|---:|---:|
| 自动化输送臂 | 4台 | 4台 | 2 m/s | 50000 元/台 |
| 工业清洗机 | 5台 | 5台 | 2 m/s | 40000 元/台 |
| 精密灌装机 | 5台 | 5台 | 2 m/s | 35000 元/台 |
| 自动传感多功能机 | 1台 | 1台 | 2 m/s | 80000 元/台 |
| 高速抛光机 | 1台 | 1台 | 2 m/s | 75000 元/台 |

设备配置的建模作用：

| 字段 | 建模含义 |
|---|---|
| 设备类型 | 决定工序可使用的资源类别 |
| 班组数量 | 决定每类设备的资源容量上限 |
| 移动速度 | 用于计算设备跨车间转运时间 |
| 单价 | 用于问题4的设备采购预算约束 |

---

## 4. 车间距离数据

距离表给出了班组到车间、车间到车间之间的距离。

### 4.1 班组到车间距离

| 起点 | 终点 | 距离 |
|---|---|---:|
| 班组1 | A | 400 m |
| 班组1 | B | 620 m |
| 班组1 | C | 460 m |
| 班组1 | D | 710 m |
| 班组1 | E | 400 m |
| 班组2 | A | 500 m |
| 班组2 | B | 460 m |
| 班组2 | C | 620 m |
| 班组2 | D | 680 m |
| 班组2 | E | 550 m |

### 4.2 车间间距离

| 起点 | 终点 | 距离 |
|---|---|---:|
| A | B | 1020 m |
| A | C | 1050 m |
| A | D | 900 m |
| A | E | 1400 m |
| B | C | 1100 m |
| B | D | 1630 m |
| B | E | 720 m |
| C | D | 520 m |
| C | E | 850 m |
| D | E | 1030 m |

距离数据的建模作用：

| 距离类型 | 用途 |
|---|---|
| 班组到车间距离 | 计算设备从班组出发到首个作业车间的初始转运时间 |
| 车间到车间距离 | 计算设备跨车间切换任务时的转运时间 |
| 同车间距离 | 按题目规则视为 0，不计算转运时间 |

---

## 5. 核心计算规则

### 5.1 工序持续时间计算

对于单设备工序，设：

- 工程量为 \(Q\)，单位为 m³；
- 分配设备台数为 \(n\)；
- 单台设备效率为 \(v\)，单位为 m³/h。

则该类设备完成该工序的作业时间为：

```math
T = \left\lceil \frac{Q}{n \cdot v} \times 3600 \right\rceil
```

其中，\(T\) 的单位为秒。

### 5.2 双设备并行工序完成时间

如果某工序需要两类设备 \(X\) 和 \(Y\) 同时参与，且两类设备都需要独立完成全部工程量，则：

```math
T_X = \left\lceil \frac{Q}{n_X \cdot v_X} \times 3600 \right\rceil
```

```math
T_Y = \left\lceil \frac{Q}{n_Y \cdot v_Y} \times 3600 \right\rceil
```

工序完成时间为：

```math
T_{op}=\max(T_X,T_Y)
```

该规则的核心含义是：两类设备并不是分摊工程量，而是各自完成完整工程量；只有两类设备均完成后，该工序才完成。

### 5.3 设备转运时间计算

设设备从位置 \(P_1\) 转运到位置 \(P_2\)，距离为 \(d\)，移动速度为 \(v_m\)，则转运时间为：

```math
T_{trans}=\left\lceil \frac{d}{v_m} \right\rceil
```

本题中，所有设备移动速度均为 2 m/s，因此：

```math
T_{trans}=\left\lceil \frac{d}{2} \right\rceil
```

关键规则：

1. 同一车间内不同工序之间转运时间忽略，记为 0。
2. 跨车间转运时间不可忽略，需要计入设备到达时间。
3. 设备从班组出发到第一个作业车间，也需要计算初始转运时间。

### 5.4 工序先后约束

每个车间内工序必须严格按编号顺序执行。

若工序 \(i\) 是工序 \(i+1\) 的前序工序，则：

```math
S_{i+1} \ge F_i
```

其中：

- \(S_i\)：工序 \(i\) 的开始时间；
- \(F_i\)：工序 \(i\) 的结束时间。

对于 C 车间循环工序，需要满足：

```math
S_{C3}^{(k+1)} \ge F_{C5}^{(k)}, \quad k=1,2
```

即后一轮 C3 必须在上一轮 C5 完成后才能开始。

---

## 6. 问题1建模思路：班组1独立完成 A 车间任务

### 6.1 问题特征

问题1只涉及：

- A车间；
- 班组1设备；
- A1、A2、A3 三道**工序级**串行（A1 全部完成后才能开始 A2，A2 全部完成后才能开始 A3）；
- 初始设备从班组1出发到 A 车间。

**层次区分（易错点）：**

- **工序之间**：A1→A2→A3 串行，不存在跨工序并行。
- **工序内部**：凡班组1拥有多台**同一设备类型**，在该工序中可**同时开工、并联作业**，等效于把该类型承担的总工程量按台数分摊，单台持续作业时间为「全工程量 /（投入台数 × 单台效率）」向上取整到秒；**表1** 中应对**每一台实际参与作业的设备**各写一行，同类型多台若并行，则起止时刻相同、持续时间为该单台分摊后的作业时长（与题目示例表1一致）。
- **双设备工序**（如 A1）：两类设备各自并行子集，该工序阶段时长为 \(\max(T_{\text{类型1}}, T_{\text{类型2}})\)；两类设备**各自**完成该工序对应的全部工程量（赛题假设(2)），通过增加台数使两类耗时尽量**平衡**，可缩短该阶段瓶颈。

问题1的本质是：在单车间、单班组、**工序链串行**的前提下，对每一道工序决策「各设备类型投入台数」（不超过班组拥有量），必要时在枚举的 \((n_{灌},n_{臂})\)、\((n_{洗},\ldots)\) 上最小化 A3 完工时刻（Makespan）；**不是**「整条产线只用一台设备串行」。

### 6.2 A车间工序计算逻辑

#### A1：缺陷填补

设备需求：精密灌装机 + 自动化输送臂。

工程量：300 m³。

班组1可用设备：

- 精密灌装机 5 台；
- 自动化输送臂 4 台。

设分配：

- \(n_{灌}\) 台精密灌装机；
- \(n_{臂}\) 台自动化输送臂。

则：

```math
T_{灌}=\left\lceil \frac{300}{n_{灌}\cdot 200}\times3600 \right\rceil
```

```math
T_{臂}=\left\lceil \frac{300}{n_{臂}\cdot 250}\times3600 \right\rceil
```

```math
T_{A1}=\max(T_{灌},T_{臂})
```

由于 A1 是第一道工序，设备从班组1到 A 车间距离为 400 m，移动速度为 2 m/s：

```math
T_{trans,班组1\to A}=\left\lceil \frac{400}{2} \right\rceil = 200s
```

因此 A1 的开始时间不早于 200s。

#### A2：表面整平

设备需求：高速抛光机 + 工业清洗机。

工程量：500 m³。

班组1可用设备：

- 高速抛光机 1 台；
- 工业清洗机 5 台。

A2 必须在 A1 完成后才能开始：

```math
S_{A2}\ge F_{A1}
```

同车间内设备转运时间为 0。

高速抛光机固定为 1 台，其时间为：

```math
T_{抛}=\left\lceil \frac{500}{1\cdot100}\times3600 \right\rceil=18000s
```

工业清洗机时间为：

```math
T_{洗}=\left\lceil \frac{500}{n_{洗}\cdot250}\times3600 \right\rceil
```

A2 完成时间为：

```math
T_{A2}=\max(T_{抛},T_{洗})
```

#### A3：强度检测

设备需求：自动传感多功能机。

工程量：500 m³。

班组1可用自动传感多功能机 1 台，效率为 100 m³/h。

```math
T_{A3}=\left\lceil \frac{500}{1\cdot100}\times3600 \right\rceil=18000s
```

A3 必须在 A2 完成后开始：

```math
S_{A3}\ge F_{A2}
```

### 6.3 问题1决策变量

对需要多台设备参与的工序 \(i\)，设分配给该工序设备类型 \(k\) 的台数为：

```math
n_{i,k}\in\mathbb{Z}^{+}
```

### 6.4 问题1约束条件

| 约束 | 表达 | 含义 |
|---|---|---|
| 设备总量约束 | \(n_{i,k}\le N_k^{(1)}\) | 每道工序中使用的同类设备台数不超过班组1拥有数量 |
| 工序先后约束 | \(S_{A2}\ge F_{A1},\ S_{A3}\ge F_{A2}\) | A车间工序串行执行 |
| 初始转运约束 | \(S_{A1}\ge 200\) | 设备从班组1到 A 车间需要 200s |

### 6.5 问题1目标函数

```math
\min F_{A3}
```

即最小化 A 车间最后一道工序 A3 的完成时间。

### 6.6 问题1求解策略

由于 A 车间工序串行，且可选设备台数有限，可以采用枚举法。

具体步骤：

1. 对 A1，枚举 \(n_{灌}=1,2,3,4,5\)，\(n_{臂}=1,2,3,4\)，选择使 \(\max(T_{灌},T_{臂})\) 最小的组合；在表1 中为所选的每台灌装机、每台输送臂各输出一行，**同一子类内**起止时间一致。
2. 对 A2，枚举 \(n_{洗}=1,2,3,4,5\)，高速抛光机台数 \(n_{抛}=1\)（班组仅 1 台），选择使 \(\max(T_{抛},T_{洗})\) 最小的组合；抛光与清洗可**同时开工**，工序完成时刻为 \(\max\)（两类设备各自的最后完工时刻），与「不考虑两类设备之间先后顺序与等待」一致。
3. A3 固定使用 1 台自动传感多功能机（班组仅 1 台）。
4. 将初始转运时间（各机到位后取可开工的公共下界）与三道工序阶段时长按链串行累加，得到总最短时长；工程实现上可在 `solve_problem1` 中对单设备类、双设备类分别枚举投入台数并更新每台设备的可用时刻。

---

## 7. 问题2建模思路：班组1完成五个车间全部任务

### 7.1 问题新增复杂性

相较于问题1，问题2增加了以下复杂度：

| 新增复杂性 | 说明 |
|---|---|
| 多车间调度 | A、B、C、D、E 五个车间都需要完成 |
| 跨车间转运 | 设备完成一个车间工序后转移到另一车间，会产生转运时间 |
| 设备竞争 | 班组1设备数量有限，不同车间可能同时争用同类设备 |
| 工序内并联 | 与问题1相同：同工序同类多台同时作业、双设备取阶段 \(\max\)；与「仅单机完成全工程量」模型有本质差别 |
| 工序依赖 | 同一车间内必须串行，不同车间之间在资源允许时可并行 |
| C车间循环 | C3→C4→C5 需要循环执行3遍 |

### 7.2 集合定义

| 符号 | 含义 |
|---|---|
| \(W=\{A,B,C,D,E\}\) | 车间集合 |
| \(J\) | 工序集合，包括 A1-A3、B1-B4、C1-C5循环展开、D1-D6、E1-E3 |
| \(K\) | 设备类型集合 |
| \(N_k\) | 班组1中设备类型 \(k\) 的台数 |

### 7.3 参数定义

| 参数 | 含义 |
|---|---|
| \(Q(j)\) | 工序 \(j\) 的工程量 |
| \(eff(j,k)\) | 工序 \(j\) 中设备类型 \(k\) 的单台效率 |
| \(dist(w_1,w_2)\) | 车间 \(w_1\) 到 \(w_2\) 的距离 |
| \(v_m\) | 设备移动速度，取 2 m/s |

### 7.3a 工序内同类多台并联与阶段时长（与问题 1 同一套规则）

问题 2 **并非**「每道工序每类设备只派一台」。与赛题假设(2)及问题 1 相同：

- 若工序 \(j\) 需要设备类型 \(k\)，且在该时刻可投入 \(n(j,k)\in\{1,\ldots,N_k\}\) 台同类设备并联，则该类设备完成工序 \(j\) 所对应工程量的作业时长为（与附件单位一致，秒向上取整）：

```math
T(j,k,n)=\left\lceil \frac{Q(j)}{n\cdot eff(j,k)}\cdot 3600\right\rceil\quad(\text{工程量单位为 }\mathrm{m^3}\text{、效率为 }\mathrm{m^3/h}\text{ 时})
```

- **双设备工序**：两类设备可同时开工，该工序阶段时长为 \(\max_k T(j,k,n(j,k))\)，工序完成时间 \(F(j)\) 为各台设备任务结束时间的最大值；结果表（表 2）中**每台**参与设备一行。
- **与问题 1 的差异**：问题 2 中同一设备类型 \(k\) 会被**不同车间、不同工序**分时复用，除单机互斥外，在任一时刻 \(t\) 活跃工序占用的同类台数须满足 \(\sum_j n(j,k)\mathbf{1}_{j\text{ 在 }t\text{ 加工}}\le N_k\)。因此 \(n(j,k)\) 的全局选取与**工序排序、跨车间转运**耦合，完整模型一般为 RCPSP 类；工程上可在列表调度/贪心定序的**每一步**对当前工序枚举可行的 \((n_1,n_2)\) 或 \(n\)，使该步工序完工最早（与 `solve_problem1` 逐步决策一致），但**整体 Makespan 仍可能为近似解**，不得与「工序内必须并联」相混淆。

### 7.4 决策变量

| 变量 | 含义 |
|---|---|
| \(S(j)\) | 工序 \(j\) 的开始时间 |
| \(F(j)\) | 工序 \(j\) 的结束时间 |
| \(n(j,k)\) | 工序 \(j\) 分配的 \(k\) 类设备台数 |
| \(x(e,j)\) | 设备 \(e\) 是否参与工序 \(j\) 的 0-1 变量 |

### 7.5 资源约束

任意时刻，同类设备的占用数量不能超过班组1拥有的总台数。

```math
\sum_{j\in active(t)} n(j,k) \le N_k,\quad \forall k
```

### 7.6 工序先后约束

若工序 \(j\) 是工序 \(j'\) 在同一车间中的前序工序，则：

```math
S(j')\ge F(j)
```

### 7.7 跨车间转运约束

设备从车间 \(w_1\) 完成任务后，转移到车间 \(w_2\) 执行下一任务时，应满足：

```math
S(j')\ge F(j)+\left\lceil \frac{dist(w_1,w_2)}{2}\right\rceil
```

设备从班组1出发到第一个作业车间时：

```math
S(j_{first,w})\ge \left\lceil \frac{dist(班组1,w)}{2}\right\rceil
```

### 7.8 目标函数

```math
\min Makespan=\max_{j\in J}F(j)
```

### 7.9 求解策略

由于该问题属于多资源受限项目调度（RCPSP）类问题，具有较高复杂度，可采用 **列表调度 / 贪心事件推进 +（可选）关键路径优先**。

基本流程：

1. **预计算工序时长下界（须含同类并联）**：对每道工序 \(j\)、每种涉及的设备类型 \(k\)，在 \(n\in\{1,\ldots,N_k\}\)（及双设备类的 \((n_1,n_2)\)）上计算阶段 \(\max\) 时长，取**枚举意义下的最短阶段工期**作为该工序的乐观工期估值；**禁止**仅用 \(n=1\) 的 `ceil(Q/eff)` 作为唯一标称时长，否则 CPM/优先级会系统性偏离真实可并行能力。
2. 采用关键路径法（CPM）或最长剩余链等规则，为「待释放工序」构造调度优先级（可与第 1 步下界加权）。
3. 在资源与车间前序约束下，反复选取可开工工序；对**当前工序**枚举并联台数并指派具体设备（与问题 1 相同的门限最小选机策略），更新每台设备的可用时刻与位置。
4. 维护每台设备的最早可用时间和当前位置；跨车间转运按距离矩阵计入。
5. 对初始调度结果可再做局部优化（邻域交换、关键块移动等）或接入 CP-SAT/MILP 精化。

---

## 8. 问题3建模思路：班组1和班组2协同完成五个车间任务

### 8.1 问题新增复杂性

相较于问题2，问题3新增：

| 新增复杂性 | 说明 |
|---|---|
| 资源池扩大 | 班组1和班组2设备均可参与调度 |
| 初始位置不同 | 班组2到各车间的初始转运时间与班组1不同 |
| 班组协作 | 两个班组设备可以并行调度 |
| 分配决策更复杂 | 需要决定工序由哪个班组或两个班组联合完成 |
| 并联池扩大 | 同型机总台数为 \(N_k^{total}\)，工序内枚举 \(n(j,k)\) 时可从两班组混选（与 §8.2a 一致）；与「各班组单机串行」模型不同 |

建模思路文档中特别指出：题目未明确限制两个班组设备是否能混用完成同一工序，因此通常可假设两个班组设备能够在同一工序中联合使用，只要设备能及时到达。若实际题目限制不允许混用，则应按限制处理。

### 8.2 设备池扩展

定义设备：

```math
E(g,k,i)
```

表示班组 \(g\) 中第 \(i\) 台类型为 \(k\) 的设备，其中 \(g=1,2\)。

总可用设备数量为：

```math
N_k^{total}=N_k^{(1)}+N_k^{(2)}
```

### 8.2a 工序内同类多台并联（双班合并资源池）

与 §7.3a **同一套时长规则**：工序 \(j\) 对设备类型 \(k\) 投入 \(n(j,k)\) 台并联时，阶段时长仍按 \(Q(j)/(n(j,k)\cdot eff(j,k))\) 向上取整到秒；双设备工序取两类阶段 \(\max\)。问题 3 与问题 2 的本质差别在于 **\(n(j,k)\le N_k^{total}\)**，且并联的 \(n(j,k)\) 台可**混编**自班组1与班组2（题目未禁止同工序混用两班组设备时），每台设备仍从各自初始节点（`班组1` / `班组2`）计算到车间的转运门限，再与问题 1 相同按门限升序取前 \(n\) 台组成并联组。表 3 须为每台参与设备输出一行，并标明**班组**。

工程实现上可将两班组设备合并为单一设备列表，复用与问题 2 相同的逐步枚举 \((n_1,n_2)\) 或 \(n\) 的调度核（`solve_problem3` → `solve_problem1`）；**不得**因「分班组叙述」而退化为每班组各派单机或忽略并联。

### 8.3 转运时间扩展

对于设备 \(E(g,k,i)\)，从位置 \(L_1\) 转移到位置 \(L_2\) 的转运时间为：

```math
T_{trans}(L_1,L_2)=\left\lceil \frac{dist(L_1,L_2)}{2}\right\rceil
```

区别在于，班组1和班组2的初始位置不同，因此初始出发时间需要分别计算。

### 8.4 设备分配决策变量

设：

```math
n_{j,k}^{(g)}
```

表示工序 \(j\) 分配班组 \(g\) 的 \(k\) 类设备台数。

### 8.5 目标函数

问题3目标函数仍为：

```math
\min Makespan=\max_{j\in J}F(j)
```

### 8.6 求解策略

建模思路文档给出三种可选求解方法；**无论采用哪一种，工序内同类并联与双设备 \(\max\) 规则必须与 §7.3a、§8.2a 一致**，CPM/距离偏好只影响「谁先排」，不能替代「每步 \(n\) 与 \((n_1,n_2)\) 枚举」。

#### 方法一：两阶段策略

第一阶段：确定各车间由哪个班组主导，或由两个班组协作。

第二阶段：在确定分配后，对每个班组分别调度，再合并结果。

**并联注意**：若第二阶段按班组**割裂**调度且禁止跨班混用同工序，则须在模型中显式写出 \(n_{j,k}^{(g)}\) 及班组互斥；若允许同工序跨班混用并联（与当前代码假设一致），则第二阶段宜合并为统一设备池再调度，否则易低估并联能力或重复计数资源。

#### 方法二：启发式策略

优先让距离目标车间更近的班组承担该车间主要工序。

根据距离表：

| 车间 | 较近班组 |
|---|---|
| A | 班组1 |
| B | 班组2 |
| C | 班组1 |
| D | 班组2 |
| E | 班组1 |

该方法可用于**构造工序优先级或初始解**，须在之后的资源分配步中仍对每道工序执行与问题 2 相同的「枚举并联台数 + 门限选机」。

#### 方法三：整数规划模型

引入 0-1 变量表示工序之间的先后顺序，通过析取图（Disjunctive Graph）构造精确调度模型，并利用分支定界法求解。变量中须包含 \(n(j,k)\) 或等价的参与机台集合，以表达同类并联对 \(F(j)\) 的影响。

---

## 9. 问题4建模思路：追加预算购买设备并进一步优化

### 9.1 问题特征

问题4在问题3基础上增加设备采购决策。

已知条件：

- 追加预算为 500000 元；
- 各类设备单价已知；
- 需要决定给班组1和班组2各增购多少台、什么类型的设备；
- 采购后重新调度，使完成全部任务时间最短。

### 9.2 决策变量

设：

```math
\Delta N_k^{(g)}
```

表示给班组 \(g\) 增购 \(k\) 类设备的台数。

要求：

```math
\Delta N_k^{(g)}\in\mathbb{Z}_{\ge0}
```

### 9.3 预算约束

```math
\sum_{g\in\{1,2\}}\sum_{k\in K}\Delta N_k^{(g)}\cdot price_k\le 500000
```

### 9.4 更新设备数量

```math
\hat{N}_k^{(g)}=N_k^{(g)}+\Delta N_k^{(g)}
```

### 9.4a 内层调度与工序内并联（增购扩展并联上界）

对每个候选采购方案，用 \(\hat{N}_k^{(g)}\) 生成设备实例列表后，计算 **\(Makespan(\hat{N})\)** 的内层调度必须与问题 3 一致：对**每一道工序**在同类设备上枚举投入台数 \(n(j,k)\)（及双设备类的 \((n_1,n_2)\)），作业时长按 §7.3a、§8.2a 的 \(\lceil Q/(n\cdot eff)\rceil\) 规则；表 4 为每台参与设备一行。**增购的经济含义**是提高 \(\sum_g \hat{N}_k^{(g)}\) 上界，使瓶颈工序可选用更大的 \(n(j,k)\) 以缩短阶段时长；若内层仍采用「每工序每类仅单机」或忽略并联，则双层模型与赛题假设(2) 不一致，瓶颈分析、边际收益 \(\Delta Makespan/price\) 也会系统性失真。

工程实现：`_expand_devices_with_purchase` 后调用 `solve_problem4`（即 `solve_problem1`），不得另写一套不含并联的简化调度仅用于 Q4。

### 9.5 目标函数

```math
\min Makespan(\hat{N}_k^{(g)})
```

问题4因此可视为一个双层优化问题：

| 层级 | 任务 |
|---|---|
| 外层 | 决定采购方案 |
| 内层 | 在采购后的设备配置下求最优调度时长 |

### 9.6 求解策略

#### 策略一：瓶颈分析

先从问题3的最优或较优调度结果中识别瓶颈设备，即关键路径上最紧张的设备类型。

优先考虑增购瓶颈设备。

#### 策略二：边际收益分析

对每类设备，计算增加 1 台设备带来的 Makespan 减少量；**\(\Delta Makespan_k\) 必须在「含工序内同类并联」的内层调度下重算**，即增购 1 台并入池后再次求 \(Makespan(\hat{N})\)，而非用单机近似或仅缩短排队时间的主观估计。

边际收益可定义为：

```math
MR_k=\frac{\Delta Makespan_k}{price_k}
```

选择边际收益更高的设备类型优先购买。

#### 策略三：枚举法

由于预算有限，最便宜设备单价为 35000 元/台，最多可购买约 14 台，因此可以枚举所有满足预算约束的采购方案。

对每种采购方案：

1. 更新班组1和班组2的设备实例列表（含 \(\Delta N_k^{(g)}\) 台新设备）；
2. 调用与问题 3 **相同**的调度内核（`solve_problem4`→`solve_problem1`：工序内枚举 \(n\) 与 \((n_1,n_2)\)、表4 逐台）；
3. 读取该方案 Makespan；
4. 在预算可行域内选择 Makespan 最小的方案（同 Makespan 时可次优目标为总采购费用最小）。

### 9.7 背包模型表达

设预算为 \(B=500000\)，设备类型 \(k\) 的单价为 \(c_k\)，采购数量为 \(d_k\)，则：

```math
\sum_k c_k d_k \le B,\quad d_k\in\mathbb{Z}_{\ge0}
```

对每种合法采购组合，调用调度算法，选择完成时间最短的组合。

---

## 10. 关键技术点汇总

### 10.1 时间精确计算

所有工序持续时间计算必须：

1. 先用小时制计算：

```math
工序时长=\frac{工程量}{台数\times效率}
```

2. 转换为秒：乘以 3600；
3. 使用 ceil 函数向上取整。

统一表达为：

```math
T=\left\lceil \frac{Q}{n\cdot v}\times3600\right\rceil
```

### 10.2 并行工序时间取最大值

双设备工序中，两类设备各自独立完成工程量，工序完成时间取两者最大值：

```math
T_{op}=\max(T_X,T_Y)
```

### 10.3 转运时间准确计算

转运时间为：

```math
T_{trans}=\left\lceil \frac{d}{2}\right\rceil
```

如果设备在时间 \(F_{L1}\) 完成位置 \(L_1\) 的任务，到达 \(L_2\) 的时间为：

```math
t_{arrive}=F_{L1}+T_{trans}(L_1,L_2)
```

该设备参与 \(L_2\) 工序的开始时间不能早于 \(t_{arrive}\)。

### 10.4 资源约束处理

调度时需要维护每台设备的状态：

| 状态 | 含义 |
|---|---|
| 最早可用时间 | 设备下一次可以参与作业的最早时间 |
| 当前所在位置 | 用于计算下一次转运时间 |
| 设备类型 | 用于匹配工序需求 |
| 所属班组 | 用于初始出发距离和问题3/4输出 |

设备分配时，优先选择满足类型要求且最早可用时间较小的设备。

### 10.5 C车间循环处理

C3-C5 重复3遍时，必须拆为 9 道独立工序，并添加链式约束：

```text
C3_1 → C4_1 → C5_1 → C3_2 → C4_2 → C5_2 → C3_3 → C4_3 → C5_3
```

对应约束包括：

```math
S_{C3,2}\ge F_{C5,1}
```

```math
S_{C3,3}\ge F_{C5,2}
```

---

## 11. 推荐方法与适用场景

| 方法 | 适用场景 | 优点 | 局限 |
|---|---|---|---|
| 枚举法 | 问题1；问题2/3 中**单道工序内**的 \((n_1,n_2)\) 或 \(n\) 台数 | 单工序阶段精确；问题1 全链可最优 | 多车间全局长枚举组合爆炸 |
| 关键路径法 CPM | 问题2/3，构建调度优先级 | 能识别关键工序链 | 属于启发式，不保证全局最优 |
| 列表调度算法 | 问题2/3，资源分配 | 实现简单，易于输出调度表 | 结果可能只是近似最优 |
| 整数规划 ILP | 精确求解 | 理论严谨，结果精确 | 变量多，计算量大 |
| 贪心 + 局部搜索 | 问题4采购决策 | 快速、可落地 | 可能陷入局部最优 |
| 模拟退火 / 遗传算法 | 大规模复杂问题 | 具备全局搜索能力 | 参数调节复杂，稳定性依赖实现 |

---

## 12. 总体建模流程

```text
输入数据
 ↓
数据预处理
 - 整理工序、设备、距离
 - 计算基本工序时间
 - 展开 C3-C5 循环工序
 ↓
问题1
 - 单车间单班组
 - 枚举设备分配
 - 计算最短 Makespan
 ↓
问题2
 - 五车间单班组
 - 引入跨车间转运约束
 - 工序内同类多台并联（与问题1同规则）+ 列表调度/贪心定序
 - 计算 Makespan（全局最优需 MILP/CP；实现上先保证并联下界正确）
 ↓
问题3
 - 五车间双班组
 - 扩展设备资源池 N_k^total，工序内同类并联可跨班组混选（与 §8.2a 一致）
 - 列表调度/贪心定序 +（可选）距离偏好或两阶段元启发式
 - 计算 Makespan；表3 输出班组字段
 ↓
问题4
 - 加入 500000 元预算约束
 - 外层：瓶颈/边际/枚举或束搜索等采购方案搜索
 - 内层：每方案下与 Q3 相同调度（**工序内同类并联** + 双设备枚举），增购扩大 \(n(j,k)\) 上界
 - 输出表5 购买方案与表4 调度；联合全局最优需 MILP/CP
```

---

## 13. 答题与实现注意事项

1. 每个问题应单独建模，变量、约束和目标函数不要混用。
2. 对双设备工序，必须明确两类设备各自独立完成工程量，工序时间取最大值。
3. 所有时间计算必须精确到秒，并向上取整。
4. 跨车间转运时间和设备初始出发时间必须纳入调度。
5. C车间 C3-C5 循环必须拆解为 9 道独立工序处理。
6. 问题4的采购方案必须分班组列出购买台数，并验证总费用不超过 500000 元。
7. 结果填表时，需要确保每台设备的起始时间、结束时间、持续时间和工序编号一致。
8. 持续工作时间应对应设备实际作业时间，不应混入设备转运时间。
9. 问题 2、问题 3、问题 4 的建模与实现须继承问题 1 的**工序内同类并联**规则；表 2、表 3、表 4 为每台参与设备一行；问题 4 评估每个购买方案时**内层调度**必须与上述规则一致，不得按「全局单机流」臆造工期。

---

## 14. 代码实现导向的建模落点

为了服务后续代码实现，本建模思路可转化为以下模块。

| 模块 | 功能 |
|---|---|
| 数据读取模块 | 读取工序流程表、班组配置表、车间距离表 |
| 工序展开模块 | 展开 C3-C5 三轮循环工序 |
| 时间计算模块 | 计算作业时间和转运时间 |
| 设备状态模块 | 维护每台设备的可用时间和当前位置 |
| 调度求解模块 | 根据问题1-4分别调度 |
| 采购优化模块 | 枚举或搜索设备购买方案 |
| 结果导出模块 | 生成表1-表5 |
| 可行性检查模块 | 检查工序顺序、设备冲突、预算约束等 |

---

## 15. 小结

该建模思路的核心是将 B 题转化为多资源受限调度问题。问题1可用小规模枚举得到全局最优 Makespan；问题2和问题3在**每一道工序上**须与问题1一致地考虑**同类多台并联**与双设备 \(\max\) 规则，再结合多车间、跨车间运输与资源冲突，采用列表调度或 CPM 定序等启发式求可行/近似最优解；问题4在**每个采购方案的内层**仍须使用同一套并联调度规则求 \(Makespan(\hat{N})\)，外层再搜索 \(\Delta N\) 与预算；采购—调度联合全局最优可通过 MILP/CP 或强启发式逼近。

整个模型的关键在于五点：

1. 双设备工序完成时间取最大值；
2. **同类多台在同一工序内并联分担工程量**（表2/表3/**表4** 逐台输出），不得默认单机；
3. 同车间工序按顺序执行；
4. 跨车间转运时间必须计入设备到达时间；
5. C3-C5 循环必须拆解成独立工序链。
